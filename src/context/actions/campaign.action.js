export const getDocuments = (params, callback) => {
    return (dispatch, getState) => {
    //   const {
    //     user: { token },
    //   } = getState();
    //   ApiClient.get(`${apiUrl}${PORT}/documents`, params, token, dispatch).then(
    //     (response) => {
    //       if (response && !response.errors) {
    //         if (response.docs) {
    //           dispatch({
    //             type: "LIST_DOCUMENT_MENU",
    //             data: response
    //           });
    //         }
    //         callback(response);
    //       } else if (response.code === 404) {
    //         toast.warn(response.message);
    //       } else {
    //         toast.error(response.errors.msg, toasterConfig);
    //       }
    //     }
    //   );
    console.log("hii actions")
    };
  };